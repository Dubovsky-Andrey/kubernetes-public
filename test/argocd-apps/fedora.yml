apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fedora-latest
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Dubovsky-Andrey/kubernetes-public
    targetRevision: HEAD
    path: test/apps/fedora

  destination:
    server: https://kubernetes.default.svc
    namespace: test

  syncPolicy:
    syncOptions:
      - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
